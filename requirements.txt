# --- Core (bắt buộc để chạy web) ---
Flask==3.0.0
flask-bcrypt==1.0.1
email-validator==2.1.0.post1
python-dotenv==1.0.1
pandas==2.1.1
openpyxl==3.1.2
authlib==1.3.2
requests==2.32.3

# --- AI optional (chatbot + vision + RAG) ---
# Cài thêm nếu dùng Gemini / YOLO / ChromaDB:
# google-genai
# chromadb==0.4.15
# ultralytics==8.0.200
# sentence-transformers
# inference-sdk  (Roboflow)
openai
